<?xml version="1.0" encoding="utf-8"?>
<!-- Menu for EventEditFragment -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Save Event -->
    <item
        android:id="@+id/action_save_event"
        android:title="@string/text_action_save"
        android:icon="@drawable/ic_rounded_save_24"
        app:iconTint="?attr/colorOnPrimary"
        app:showAsAction="ifRoom" />

    <!-- Cancel Edit -->
    <item
        android:id="@+id/action_cancel_edit"
        android:title="@string/text_action_cancel"
        android:icon="@drawable/ic_rounded_cancel_24"
        app:iconTint="?attr/colorOnPrimary"
        app:showAsAction="never" />

</menu>