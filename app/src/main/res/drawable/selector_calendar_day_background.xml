<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Selected state -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/calendar_day_selected" />
            <stroke android:width="2dp" android:color="@color/calendar_day_selected_stroke" />
            <corners android:radius="@dimen/radius_s" />
        </shape>
    </item>

    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/calendar_day_pressed" />
            <corners android:radius="@dimen/radius_s" />
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="@dimen/radius_s" />
        </shape>
    </item>

</selector>