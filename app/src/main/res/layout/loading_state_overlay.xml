<?xml version="1.0" encoding="utf-8"?>
<!--
Loading State Overlay Layout

Simple loading overlay with circular progress indicator for MVVM operations.
Used across LocalEvents Activity for various async operations like loading,
importing, and exporting events.

Features:
- Semi-transparent background
- Centered circular progress indicator
- Simple and lightweight
- Compatible with CoordinatorLayout anchoring
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurfaceVariant"
    android:clickable="true"
    android:focusable="true"
    android:visibility="gone">

    <!-- Loading Indicator Container -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/spacing_l">

        <!-- Circular Progress Indicator -->
        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/progress_indicator"
            android:layout_width="54dp"
            android:layout_height="54dp"
            android:indeterminate="true"
            app:indicatorColor="?attr/colorPrimary"
            app:trackColor="?attr/colorOnSurface"
            app:trackCornerRadius="4dp" />
<!--        android:layout_width="@dimen/progress_indicator_size"-->
<!--        android:layout_height="@dimen/progress_indicator_size"-->
<!--        app:trackCornerRadius="@dimen/progress_track_corner_radius" />-->

        <!-- Loading Text (Optional - hidden by default) -->
        <TextView
            android:id="@+id/loading_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_m"
            android:text="@string/loading_loading"
            android:textAppearance="?attr/textAppearanceBody2"
            android:textColor="?attr/colorOnSurface"
            android:visibility="gone" />

    </LinearLayout>

</FrameLayout>